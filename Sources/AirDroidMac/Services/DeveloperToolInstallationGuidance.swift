enum DeveloperToolInstallationGuidance {
    static let adbUnavailable =
        "ADB is not installed. Install Android Platform Tools, then reopen the app. Homebrew: brew install --cask android-platform-tools."

    static let scrcpyUnavailable =
        "Stock scrcpy 4.1 is not installed. Install scrcpy, then reopen the app. Homebrew: brew install scrcpy."
}
